{#
    Variables
	img1: background image
  gradient: 1 apply gradient and 0 not apply
	text1: text
	text2: link text
  modal_text: text for popup
	button_color: style of button
	color_fondo: background color
  customHeight: Custom Height for block
  height: custom class for height.
  customStyle: Personal class
  ocultar: dispositivos en los que el bloque no se mostrará. Opciones: 'escritorio', 'movil', 'tablet'
#}

{% set build_texto1 = {
    '#type':   'processed_text',
    '#text':    text1.value,
    '#format': 'rich_text',
  }
%}

{%
 set modal_id = random()
%} 

{% if gradient == 1 %}
  {% set style_gradient = 
    'linear-gradient(rgba(0, 0, 0, 0.5),rgba(0, 0, 0, 0.5)),'
  %}
{% endif %}

<div class="consum-popup-custom cta {{ background_color }} {{ customStyle }} {% if height %} {{ height }} {% endif %}
{% if ocultar['escritorio'] %}  d-lg-none d-xl-none {% else %} d-lg-block d-xl-block  {% endif %}
{% if ocultar['tablet'] %} d-md-none {% else %} d-md-block {% endif %}
{% if ocultar['movil'] %} d-none d-sm-none {% else %} d-xs-block d-sm-block {% endif %}
"style="{% if customHeight  %} min-height: {{ customHeight }};{% endif %} background-image: {% if style_gradient %} {{ style_gradient }} {% endif %} url('{{ img1 }}'); {% if alto  %} padding:0px!important; {% endif %}">
  
  <div class="row justify-content-center align-items-center" >
    <div class="col-12">
      <h4 class="{% if color_fondo == 'sin-fondo' or color_fondo == 'fondo-blanco' %} gris3  {% endif %}">
        {{ build_texto1 }}
       </h4>      
        <br />
        <a type="button" class="{{ button_color }}" data-toggle="modal" data-target="#customModal-{{ modal_id }}" role="button">{{ text2 }}</a>
      
      {% if modal_text.value %}
      <div class="modal" id="customModal-{{ modal_id }}">
        <div class="modal-dialog modal-dialog-centered">
          <div class="modal-content">
            <div class="modal-body">
              {{ modal_text.value|raw }}
            </div>           
          </div>
        </div>
      </div>
      {% endif %}

    </div>
  </div>

</div>


